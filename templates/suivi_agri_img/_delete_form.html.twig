<form method="post" action="{{ path('suivi_agri_img_delete', {'id': suivi_agri_img.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_agri_img.id) }}">
    <button class="btn">Delete</button>
</form>
