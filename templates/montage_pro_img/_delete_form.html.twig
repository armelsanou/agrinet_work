<form method="post" action="{{ path('montage_pro_img_delete', {'id': montage_pro_img.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ montage_pro_img.id) }}">
    <button class="btn">Delete</button>
</form>
