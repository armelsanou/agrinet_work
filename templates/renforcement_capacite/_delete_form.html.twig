<form method="post" action="{{ path('renforcement_capacite_delete', {'id': renforcement_capacite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ renforcement_capacite.id) }}">
    <button class="btn">Delete</button>
</form>
