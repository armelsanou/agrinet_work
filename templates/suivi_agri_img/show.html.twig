{% extends 'base.html.twig' %}

{% block title %}SuiviAgriImg{% endblock %}

{% block body %}
    <h1>SuiviAgriImg</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suivi_agri_img.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ suivi_agri_img.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('suivi_agri_img_index') }}">back to list</a>

    <a href="{{ path('suivi_agri_img_edit', {'id': suivi_agri_img.id}) }}">edit</a>

    {{ include('suivi_agri_img/_delete_form.html.twig') }}
{% endblock %}
