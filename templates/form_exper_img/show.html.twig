{% extends 'base.html.twig' %}

{% block title %}FormExperImg{% endblock %}

{% block body %}
    <h1>FormExperImg</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_exper_img.id }}</td>
            </tr>
            <tr>
                <th>Image1</th>
                <td>{{ form_exper_img.image1 }}</td>
            </tr>
            <tr>
                <th>Image2</th>
                <td>{{ form_exper_img.image2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('form_exper_img_index') }}">back to list</a>

    <a href="{{ path('form_exper_img_edit', {'id': form_exper_img.id}) }}">edit</a>

    {{ include('form_exper_img/_delete_form.html.twig') }}
{% endblock %}
