{% extends 'base.html.twig' %}

{% block title %}FormationInitImg{% endblock %}

{% block body %}
    <h1>FormationInitImg</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation_init_img.id }}</td>
            </tr>
            <tr>
                <th>Image1</th>
                <td>{{ formation_init_img.image1 }}</td>
            </tr>
            <tr>
                <th>Image2</th>
                <td>{{ formation_init_img.image2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formation_init_img_index') }}">back to list</a>

    <a href="{{ path('formation_init_img_edit', {'id': formation_init_img.id}) }}">edit</a>

    {{ include('formation_init_img/_delete_form.html.twig') }}
{% endblock %}
