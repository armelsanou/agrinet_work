<form method="post" action="{{ path('strategie_action_img_delete', {'id': strategie_action_img.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strategie_action_img.id) }}">
    <button class="btn">Delete</button>
</form>
