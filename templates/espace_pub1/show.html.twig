{% extends 'base.html.twig' %}

{% block title %}EspacePub1{% endblock %}

{% block body %}
    <h1>EspacePub1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espace_pub1.id }}</td>
            </tr>
            <tr>
                <th>Photo1</th>
                <td>{{ espace_pub1.photo1 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('espace_pub1_index') }}">back to list</a>

    <a href="{{ path('espace_pub1_edit', {'id': espace_pub1.id}) }}">edit</a>

    {{ include('espace_pub1/_delete_form.html.twig') }}
{% endblock %}
