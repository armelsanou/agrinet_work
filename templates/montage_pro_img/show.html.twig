{% extends 'base.html.twig' %}

{% block title %}MontageProImg{% endblock %}

{% block body %}
    <h1>MontageProImg</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ montage_pro_img.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ montage_pro_img.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('montage_pro_img_index') }}">back to list</a>

    <a href="{{ path('montage_pro_img_edit', {'id': montage_pro_img.id}) }}">edit</a>

    {{ include('montage_pro_img/_delete_form.html.twig') }}
{% endblock %}
