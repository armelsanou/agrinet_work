security:
    encoders:
        App\Entity\User:
            algorithm: bcrypt
    role_hierarchy:       
        ROLE_ADMIN: ROLE_USER

    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        in_memory: { memory: ~ }
        in_database:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true
            provider: in_database

            form_login:
                login_path: security_login
                check_path: security_login

            logout:
                path: security_logout
                target: /
                
        #api:
         #   pattern: ^/
           # stateless: true
          #  guard:
            #   authenticators:
               # - App\Security\TokenAuthenticator
             #     - token_authenticator
            # activate different ways to authenticate

            # http_basic: true
            # https://symfony.com/doc/current/security.html#a-configuring-how-your-users-will-authenticate

            # form_login: true
            # https://symfony.com/doc/current/security/form_login_setup.html

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
         - { path: ^/recap_elevage, roles: ROLE_USER }
         - { path: ^/login, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/phytopharmacie, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/contact, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/a_propos, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/vulgarisation_agricole, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/formation_expertise, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/devenir_expert, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/formation_initiale, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/bibliotheque_agricole, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/mieux_produire, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/formation_pratique, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/gestion_du_sol, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/home, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/recap_culture, roles: ROLE_USER }
         - { path: ^/montage_projets, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/agri_impact, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/welcome, role: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/actualite,  role: IS_AUTHENTICATED_ANONYMOUSLY }
         #- { path: ^/, roles: IS_AUTHENTICATED_FULLY }
