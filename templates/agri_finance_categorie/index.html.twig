{% extends 'base.html.twig' %}

{% block title %}AgriFinanceCategorie index{% endblock %}

{% block body %}
    <h1>AgriFinanceCategorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Categorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for agri_finance_categorie in agri_finance_categories %}
            <tr>
                <td>{{ agri_finance_categorie.id }}</td>
                <td>{{ agri_finance_categorie.categorie }}</td>
                <td>
                    <a href="{{ path('agri_finance_categorie_show', {'id': agri_finance_categorie.id}) }}">show</a>
                    <a href="{{ path('agri_finance_categorie_edit', {'id': agri_finance_categorie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('agri_finance_categorie_new') }}">Create new</a>
{% endblock %}
